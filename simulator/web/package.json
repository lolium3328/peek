{
  "name": "@peek/simulator-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "bun run build && bun --hot server/index.ts",
    "start": "bun server/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  }
}
